import 'package:flutter/material.dart';

@immutable
class IncrementButtonMwIncrementState {
  final int counterValue;
  final double counterSize;

  IncrementButtonMwIncrementState(this.counterValue, this.counterSize);
}
